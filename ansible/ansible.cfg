[defaults]
# Tell Ansible not to check SSH host keys for localhost
# (We're managing localhost, not remote servers)
host_key_checking = False

# Log everything — important for audit trail in self-healing systems
log_path = /tmp/ansible.log

# Don't create .retry files (clutters directory)
retry_files_enabled = False

# Show diffs when files change
diff = True
